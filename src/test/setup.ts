/**
 * Vitest setup file
 */

import '@testing-library/jest-dom';

// Global test setup
// Note: Vitest automatically mocks console methods, no need for jest.fn()
// Uncomment if you want to suppress console warnings in tests
// import { vi } from 'vitest';
// global.console = {
//   ...console,
//   error: vi.fn(),
//   warn: vi.fn(),
// };
